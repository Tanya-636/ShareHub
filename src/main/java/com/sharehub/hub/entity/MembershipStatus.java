package com.sharehub.hub.entity;

public enum MembershipStatus {
    ACTIVE,   // The user is an active member (used immediately upon admin addition)
    REMOVED   // The member was removed or left the group
}